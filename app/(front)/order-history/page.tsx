import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Order History",
};

const OrderHistory = () => {
  return <section className="container">OrderHistory</section>;
};

export default OrderHistory;
