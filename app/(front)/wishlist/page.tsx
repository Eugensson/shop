import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Wishlist",
};

const Wishlist = () => {
  return <section className="container">Wishlist</section>;
};

export default Wishlist;
