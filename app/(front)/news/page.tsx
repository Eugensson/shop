import { Metadata } from "next";

export const metadata: Metadata = {
  title: "News",
};

const News = () => {
  return <section className="container py-10 h-full">News</section>;
};

export default News;
