import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Services",
};

const Services = () => {
  return <section className="container py-10 h-full">Services</section>;
};

export default Services;
