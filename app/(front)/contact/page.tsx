import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact",
};

const Contact = () => {
  return <section className="container py-10 h-full">Contact</section>;
};

export default Contact;
